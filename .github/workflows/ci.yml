name: Say hello

on:
  push:
    branches:
      - master

jobs:
  hello_world:
    runs-on: ubuntu-20.04
    steps:
      - name: Echo the message
        run: |
          echo "Hello World!"
      - name: Dime la fecha
        run: date
      - run: ls -l